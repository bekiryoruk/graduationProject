import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    height: 42,
    margin: 12,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    width: '95%',
  },
});

export default styles;
