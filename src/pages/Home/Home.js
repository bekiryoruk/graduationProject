import React from 'react';
import {View, Text, ScrollView} from 'react-native';

const Home = () => {
  return (
    <View>
      <ScrollView>
        <Text>Home Page</Text>
      </ScrollView>
    </View>
  );
};

export default Home;
